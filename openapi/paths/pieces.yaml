# ─── Piece Endpoints ──────────────────────────────────────────

# GET + POST /orders/{order_id}/pieces
collection:
  get:
    summary: List all pieces for an order
    operationId: listPieces
    tags:
      - Pieces
    security:
      - bearerAuth: []
    parameters:
      - name: order_id
        in: path
        required: true
        description: The order's UUID
        schema:
          type: string
          format: uuid
      - name: page
        in: query
        required: false
        description: Page number (1-based). Defaults to 1.
        schema:
          type: integer
          minimum: 1
          default: 1
          example: 1
      - name: page_size
        in: query
        required: false
        description: Number of items per page. Defaults to 20, max 100.
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
          example: 20
    responses:
      "200":
        description: Paginated list of pieces
        content:
          application/json:
            schema:
              $ref: "../schemas/piece.yaml#/PaginatedPieceResponse"
      "400":
        description: Invalid pagination parameters (e.g. page < 1, page_size not in 1–100)
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "page_size must be between 1 and 100"
      "401":
        description: Missing or invalid token
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "authorization header required"
      "404":
        description: Order not found
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "order not found"

  post:
    summary: Create a new piece for an order
    operationId: createPiece
    tags:
      - Pieces
    security:
      - bearerAuth: []
    parameters:
      - name: order_id
        in: path
        required: true
        description: The order's UUID
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../schemas/piece.yaml#/CreatePieceRequest"
          example:
            title: "Side panel"
            definition_id: "a0000000-0000-0000-0000-000000000001"
            dimensions:
              Length: 150.5
              Width: 80.0
            quantity: 15
    responses:
      "201":
        description: Piece created successfully
        content:
          application/json:
            schema:
              $ref: "../schemas/piece.yaml#/PieceResponse"
      "400":
        description: Invalid request body
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "invalid request body"
      "401":
        description: Missing or invalid token
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "authorization header required"
      "404":
        description: Order not found
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "order not found"

# GET/PUT/DELETE /orders/{order_id}/pieces/{piece_id}
item:
  get:
    summary: Get a piece by ID
    operationId: getPiece
    tags:
      - Pieces
    security:
      - bearerAuth: []
    parameters:
      - name: order_id
        in: path
        required: true
        description: The order's UUID
        schema:
          type: string
          format: uuid
      - name: piece_id
        in: path
        required: true
        description: The piece's UUID
        schema:
          type: string
          format: uuid
    responses:
      "200":
        description: Piece found
        content:
          application/json:
            schema:
              $ref: "../schemas/piece.yaml#/PieceResponse"
      "400":
        description: Invalid ID format
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "invalid piece ID format"
      "401":
        description: Missing or invalid token
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "authorization header required"
      "404":
        description: Resource not found
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "piece not found"

  put:
    summary: Update a piece
    operationId: updatePiece
    tags:
      - Pieces
    security:
      - bearerAuth: []
    parameters:
      - name: order_id
        in: path
        required: true
        description: The order's UUID
        schema:
          type: string
          format: uuid
      - name: piece_id
        in: path
        required: true
        description: The piece's UUID
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../schemas/piece.yaml#/UpdatePieceRequest"
          example:
            title: "Side panel (revised)"
            dimensions:
              Length: 200.0
              Width: 100.0
            quantity: 20
    responses:
      "200":
        description: Piece updated successfully
        content:
          application/json:
            schema:
              $ref: "../schemas/piece.yaml#/PieceResponse"
      "400":
        description: Invalid request body or ID format
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "invalid piece ID format"
      "401":
        description: Missing or invalid token
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "authorization header required"
      "404":
        description: Resource not found
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "piece not found"

  delete:
    summary: Delete a piece
    operationId: deletePiece
    tags:
      - Pieces
    security:
      - bearerAuth: []
    parameters:
      - name: order_id
        in: path
        required: true
        description: The order's UUID
        schema:
          type: string
          format: uuid
      - name: piece_id
        in: path
        required: true
        description: The piece's UUID
        schema:
          type: string
          format: uuid
    responses:
      "204":
        description: Piece deleted successfully
      "400":
        description: Invalid ID format
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "invalid piece ID format"
      "401":
        description: Missing or invalid token
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "authorization header required"
      "404":
        description: Resource not found
        content:
          application/json:
            schema:
              $ref: "../schemas/error.yaml#/ErrorResponse"
            example:
              error: "piece not found"
